# Main training configuration. Edit this file, then run: python train.py

experiment:
  name: aodraw_yolov8s_baseline
  stage: baseline
  module: baselines
  note: initial baseline

train:
  model: weights/pretrained/yolov8s.pt
  data: datasets_yolo/AODRaw_sRGB_YOLO/aodraw.yaml
  epochs: 100
  imgsz: 640
  batch: 16
  device: 0
  workers: 8
  seed: 42
  optimizer: auto
  lr0: 0.01
  patience: 50
  pretrained: true
  exist_ok: true

output:
  project_root: runs
  save_period: -1
